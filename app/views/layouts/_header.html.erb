<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to "Home", root_path, class: "btn btn-light nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "All users", users_path, class: "btn btn-light nav-link" %>
      </li>
      <% unless user_signed_in? %>
      <li class="nav-item">
        <%= link_to "Log in", new_user_session_path, class: "btn btn-light nav-link" %>
      </li>
      <% end %>
      <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to "My page", current_user, class: "btn btn-light nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "btn btn-light nav-link" %>
      </li>
      <% end %>
    </ul>
      <form class="form-inline">
      <%= search_form_for @search, url:root_path do |f| %>
        <%= f.search_field :content_cont, class: "form-control mr-sm-2", placeholder:"  keyword..." %>
        <%= f.submit "Serch", class: "btn btn-light"%>
      <% end %>
    </form>
  </div>
</nav>
